# ThingStruct PRD

- 版本：v0.3
- 日期：2026-08-07
- 状态：产品收敛基线
- 文档职责：定义产品目标、目标用户、核心体验、当前边界与验证标准

如果本文档与其他文档在产品目标、优先级或范围上冲突，以本文档为准。`README.md` 负责领域模型和算法，`Design.md` 负责 UI 表达，`SystemSurfaces.md` 与 `weiget.md` 负责系统入口的阶段性方案。

## 1. 决策摘要

ThingStruct 当前只验证一个产品假设：

> 对于拥有少数几种重复日型、却容易在执行中失去上下文的人，一次设置好日型，并在一天中持续告诉他“现在是什么、接下来做什么”，能够减少重复规划和临场决策的负担。

由此确定以下边界：

- `Now` 是产品核心。
- 模板是支撑 `Now` 的基础设施，不是用户最终目的。
- 默认日型自动运行；模板切换用于处理例外，不把每日显式选择设为必经步骤。
- 手机端优先负责查看和执行，只保留完成核心闭环所需的创建与修正。
- 当前只验证 App 内核心闭环；Widget 是验证成功后的第一个放大器。
- 已实现但不属于当前验证范围的能力可以保留，不继续扩展，不得反向扩大产品目标。

## 2. 一句话定义

ThingStruct 帮助拥有几种重复日型、却容易在执行中失去上下文的人，不必每天重新规划，也能随时知道现在该做什么。

对外表达优先使用这句话，不使用“日常运行系统”“routine OS”等宽泛类别代替具体价值。

## 3. 用户问题

目标用户通常并不缺少计划能力。他们的问题是：

- 一天的基本结构经常重复，但每天仍要重新组织一次。
- 在工作、休息、通勤、健身等状态之间切换时，容易忘记原本的节奏。
- 普通 todo 只告诉用户“还有什么没做”，不能回答“此刻应该关注什么”。
- 日历擅长表达事件，却不适合承载随场景重复出现的短 checklist 和自我提示。
- 复杂规划工具本身会产生维护成本。

ThingStruct 要减少的是重复决策和上下文恢复成本，而不是增加任务完成数量。

## 4. 目标用户

### 4.1 首要用户

当前只服务这一类用户：

- 使用 iPhone 作为主要随身设备。
- 一周内存在 2–5 种可复用日型，例如普通工作日、居家办公日、加班日、恢复日。
- 日型大体稳定，但执行会因上下文切换或临时变化而漂移。
- 希望看到少量、与当前状态直接相关的步骤，而不是维护通用任务收集箱。
- 愿意先设置一次结构，以换取后续更低的每日维护成本。

“喜欢 routine”“喜欢柔和提醒”只能作为特征，不能单独构成目标用户定义。

### 4.2 非目标用户

- 主要需要收集一次性事项的用户。
- 主要依赖外部日历事件驱动一天的用户。
- 需要团队协作、任务分配或项目管理的用户。
- 需要跨平台数据库和传统云同步的用户。
- 希望在手机上进行复杂排程和精细日历编排的用户。
- 主要诉求是习惯打卡、连续天数或游戏化激励的用户。

## 5. 核心价值与产品原则

### 5.1 核心价值

> 一次设置，全天少做决定。

### 5.2 产品原则

1. `Now` 优先：所有功能首先回答它是否让“现在该做什么”更清楚。
2. 自动运行：有明确默认值时直接运行，不要求用户每天重复确认。
3. 例外优先于编排：今天不同时允许快速切换，而不是迫使用户编辑规则。
4. 当前上下文优先：只展示与此刻相关的少量信息。
5. 执行优先于创建：创建和编辑只服务首次激活与必要修正。
6. 本地为真：用户确认后的结构与完成状态以本地文档为唯一真相。
7. 温和可见：提醒和系统入口用于降低查找成本，不制造新的打断。
8. 能力不等于范围：已有代码能力只有在核心闭环被验证后才能成为产品承诺。

## 6. 核心概念

### 6.1 日型模板

日型模板是一种可复用的一天，包含：

- 一组有时间范围的状态块。
- 每个状态下可选的短 checklist。
- 每个状态下可选的短提示。

模板是基础设施。用户不需要理解候选窗口、物化、层级索引、override 优先级等内部术语。

### 6.2 今日计划

今日计划是某个模板在具体日期上的运行实例。用户对今天的完成状态和临时修正不会自动改写原模板。

### 6.3 当前状态

当前状态是此刻生效的 block。它必须能稳定回答：

- 我现在处于什么状态？
- 这个状态到什么时候？
- 我现在最多需要关注哪几个步骤？
- 接下来是什么？

### 6.4 Checklist

Checklist 是某个重复状态下的执行步骤，不是通用 todo：

- 绑定到具体 block。
- 标题短、动作明确。
- 默认数量克制。
- 不提供 inbox 心智。

## 7. 核心用户旅程

### 7.1 首次激活

新用户必须能够在没有样例数据、没有历史记录、不了解内部模型的前提下开始：

1. 从少量起步方式中选择一个：使用起步模板、创建简单日型、导入已有结构。
2. 只填写运行第一天必需的信息。
3. 为模板设置适用日，或设为默认模板。
4. 立即生成并进入今天的 `Now`。

首次激活不要求用户先积累三天计划，也不要求先理解 Suggested、Saved、Schedule 的关系。

### 7.2 日常启动

- 如果今天存在明确默认模板，自动生成并运行今天。
- `Now` 直接打开当前状态，不先展示模板选择器。
- 用户只有在“今天不同”时才主动切换模板或选择无模板日。
- 如果不存在任何可运行模板，进入首次激活或恢复流程，而不是填充生产样例数据。

### 7.3 日常执行

用户主要进行以下动作：

- 看当前状态与结束时间。
- 查看当前状态下最多 1–3 个优先步骤。
- 一次点按完成一个步骤。
- 必要时查看下一状态。

用户不应为了完成以上动作进入模板库、时间轴编辑器或系统设置。

### 7.4 处理例外

今天与默认结构不同时，优先提供：

1. 切换为另一个已有日型。
2. 选择“今天不运行模板”。
3. 对今天做少量临时修正。

临时修正默认只影响今天。是否沉淀回模板属于后续动作，不在修正当下强迫用户决定。

### 7.5 一天结束

一期不要求完整复盘系统。可以在用户主动访问时提供轻量入口：

- 保留今天的完成状态。
- 将有价值的临时结构另存为模板。
- 明确区分“只改今天”和“更新以后”。

## 8. 当前 MVP 范围

### 8.1 P0：必须完成并验证

#### A. 真实首次激活

- 生产环境空文档启动。
- 提供可理解的起步模板或简化创建流程。
- 用户能在 5 分钟内得到一个可运行的今天。
- sample data 只能用于开发、Preview 和自动化测试。

#### B. 默认运行与例外切换

- 有默认模板时自动运行今天。
- 允许一键进入“今天不同”并切换模板。
- 切换前明确是否会覆盖今天的完成状态或临时修正。
- weekday default 是内部选择依据，不作为每日必经的管理界面。

#### C. `Now`

- 默认首页。
- 展示当前 block、结束时间、当前未完成 checklist 和下一 block。
- 未完成步骤优先，默认可见数量不超过 3 个。
- 一次点按完成步骤。
- 对无计划、空白时段、全部完成和加载失败提供明确状态。

#### D. `Today`

- 展示今天的整体结构与当前所在位置。
- 支持查看 block 详情。
- 只保留处理当天例外所需的轻量修正。
- 复杂层级编排、自由拖拽和高频创建不是 P0。

#### E. 本地持久化

- 今日计划、模板和完成状态可靠保存。
- 重启或回到前台后恢复一致状态。
- 本地文档是唯一业务真相。

### 8.2 P1：P0 验证通过后

- 一个 `Now` Widget，优先支持 `systemSmall`，按需要增加 `systemMedium`。
- Widget 内完成当前 checklist。
- 简单的默认日设置。
- 将今天的有效修改保存为模板。
- 克制的本地通知。

### 8.3 冻结能力

以下能力即使已有实现，也不继续新增需求、扩展形态或进行体验打磨：

- 多层 overlay 的继续扩展。
- 最近三天自动候选模板作为主路径。
- 复杂 weekday rule、date override 和 regenerate 管理界面。
- YAML 导入导出作为普通用户主路径。
- AI 或云端编排。
- Lock Screen accessory widgets。
- Live Activities。
- Controls、Siri、Spotlight、App Shortcuts。
- Home Screen Quick Actions。
- Notification Actions。
- 更多主题、外观配置和非必要自定义。

冻结不等于删除。只有验证结果证明某项能力能明显改善核心指标，才重新进入排期。

### 8.4 明确不做

- 通用 todo inbox。
- 日历同步和会议调度。
- 团队协作与共享任务空间。
- 项目管理。
- 传统云同步。
- 长文本笔记系统。
- 以连续打卡或积分为核心的激励系统。

## 9. 信息架构

### 9.1 `Now`

产品首页与核心执行页，优先回答：

1. 现在是什么？
2. 现在需要做什么？
3. 什么时候进入下一状态？

### 9.2 `Today`

今天的结构查看器和例外修正入口，优先回答：

1. 今天整体如何安排？
2. 当前处于哪里？
3. 今天不同时如何最小修改？

### 9.3 `Library`

低频基础设施入口，包含模板、默认日设置和诊断性导入导出。Library 不承担日常启动流程。

## 10. 成功标准

### 10.1 北极星指标

`每周运行天数`：用户在某天拥有有效今日计划，并至少查看一次 `Now` 或完成一次当前 checklist。

### 10.2 激活指标

- 80% 的测试用户无需口头教学，在 5 分钟内建立并运行第一个真实日型。
- 首次激活后直接进入可用的 `Now`，而不是停留在模板管理页面。

### 10.3 留存与使用指标

- 至少 60% 的测试用户在首个 7 天中运行 4 天以上。
- 用户每日维护结构的中位时间低于 1 分钟。
- `Now` 的使用明显高于模板管理和结构编辑。
- 模板切换主要发生在例外日，而不是成为每天的强制操作。

### 10.4 体验指标

7 天访谈中，用户能够主动表达至少一项结果：

- 少了一次重新规划。
- 更快恢复当前上下文。
- 更少打开多个 App 查找下一步。
- 提醒是有帮助的，而不是增加压力。

### 10.5 护栏指标

- 不以新增 task 数量衡量成功。
- 不以模板数量衡量成功。
- 不以系统入口数量衡量成功。
- 如果用户需要频繁修改时间轴才能继续使用，视为产品失败信号，而不是编辑功能机会。

## 11. 验证计划

在继续新增功能前执行一次 7 天验证：

1. 使用空白生产数据，不注入 sample data。
2. 招募至少 5 位符合首要用户定义的测试者。
3. 不做口头教学，观察首次激活。
4. 记录激活耗时、每周运行天数、每日编辑耗时和 `Now` 使用情况。
5. 第 7 天访谈“是否减少重新决定下一步的次数”。

通过条件：

- 至少 4/5 用户独立完成首次激活。
- 至少 3/5 用户运行 4 天以上。
- 日常维护中位时间低于 1 分钟。
- 至少 3/5 用户能明确描述减少决策或恢复上下文的价值。

未达到条件时，优先修改首次激活、默认运行或 `Now`，不增加新的系统入口。

## 12. 需求准入规则

任何新需求进入开发前必须回答：

1. 它服务哪个目标用户问题？
2. 它发生在首次激活、默认运行、日常执行还是例外处理？
3. 它如何改善北极星指标或护栏指标？
4. 不做它，核心闭环是否仍能成立？
5. 是否可以先用更小的产品实验验证？

无法回答以上问题的需求默认不进入开发。

每条被接受的需求至少包含：

- 用户触发条件。
- 成功路径。
- 空态与错误恢复。
- 对已有今日计划的影响。
- 可观察的验收标准。
- 明确的非目标。

## 13. 当前实现与目标状态

当前代码已包含模板引擎、分层 block、通知、Widget、Live Activity、Controls、Shortcuts、Quick Actions 和导入导出等能力。这些实现可以作为技术资产保留，但不表示它们都属于当前产品范围。

当前最重要的产品差距是：

- 生产首次启动仍依赖 sample data 展示完整体验。
- 模板的创建与首次运行之间存在冷启动断点。
- 每日显式选择与 weekday 自动规则的心智冲突尚未按本 PRD 收敛。
- `Now` 尚未被作为唯一核心结果来衡量。

后续实现应先让代码与本 PRD 的 P0 行为对齐，再决定是否恢复冻结能力。

## 14. 最终产品主张

ThingStruct 不帮助用户管理更多事情。

它把少数几种重复的一天保存下来，在合适的时刻只告诉用户当前最需要知道的内容。

> `Now` 是产品，模板是基础设施，系统入口是放大器，AI 是未来能力。
