# Samoyed System Surfaces

- 版本：v0.3
- 日期：2026-08-07
- 状态：冻结能力清单与后续进入条件

本文档记录 Widget、Live Activities、Controls、Shortcuts、Quick Actions 和通知动作等 iOS 系统入口的产品边界。

[PRD.md](PRD.md) 定义产品范围。系统入口只能放大已经验证的核心价值，不能用入口数量替代核心闭环验证。

## 1. 当前产品决策

在 P0 验证完成前：

- 不新增系统入口。
- 不扩展现有系统入口的形态、动作和配置。
- 不以系统入口覆盖面作为一期完成标准。
- 只修复会导致数据错误、崩溃或阻断 P0 的问题。
- 首先验证 App 内的“激活 → 默认运行 → Now → 完成步骤 → 处理例外”闭环。

P0 验证通过后，第一个进入 P1 的系统入口是一个 `Now` Widget。其他入口继续冻结，逐项证明价值后再恢复。

## 2. 产品定位

系统入口不是独立产品。它们只服务两个已经存在的核心需求：

1. `看`：更低成本地知道当前状态和当前步骤。
2. `做`：更低成本地完成一个明确的当前步骤。

系统入口不得承担：

- 创建日型。
- 编辑 block 时间或层级。
- 切换会覆盖今天的模板。
- 输入任意文本。
- 管理 schedule、override 或未来日期。
- 暴露内部 ID 和领域术语。

## 3. 能力状态

| 系统入口 | 当前代码状态 | 产品状态 | 重新进入条件 |
| --- | --- | --- | --- |
| Home Screen `Now` Widget | 已实现基础能力 | P1 候选 | P0 验证通过，且用户需要更低成本查看 `Now` |
| Lock Screen accessory widgets | 已有或可复用基础 | 冻结 | Home Screen Widget 已证明持续使用价值 |
| Live Activities | 已实现基础能力 | 冻结 | 存在明确、持续且有起止的状态追踪需求 |
| Controls | 已实现基础能力 | 冻结 | 单一动作在 Widget/App 内已被高频使用 |
| App Shortcuts / Siri / Spotlight | 已实现基础能力 | 冻结 | 用户主动请求自动化，且动作语义稳定 |
| Home Screen Quick Actions | 已实现基础能力 | 冻结 | 能显著缩短已验证主路径 |
| Notification Actions | 已实现基础能力 | 冻结 | 本地提醒本身被证明有帮助且不造成压力 |
| Watch、CarPlay、Mac 等派生位置 | 未作为独立产品验证 | 不立项 | 对应主入口在 iPhone 上先成立 |

“已实现”只表示技术资产存在，不表示属于当前产品承诺。

## 4. 统一交互模型

### 4.1 查询

系统入口的查询快照只包含渲染所需的最小信息：

- 当前 block 标题
- 当前时间范围
- 当前最多 1–3 个未完成 checklist
- 剩余数量
- 下一 block 的可选摘要
- 无计划、空白时段或全部完成状态

查询结果必须与 App 内 `Now` 使用同一套核心推导，不能为某个入口另写业务规则。

### 4.2 命令

系统入口只允许轻量、确定、可容错的动作：

- 打开 `Now`
- 打开当前 block
- 完成明确的一条当前 checklist

冻结能力恢复前，不增加以下公开动作：

- 切换模板
- 创建或编辑 block
- 修改 schedule
- 启动复杂自动化

### 4.3 写入原则

- 所有写入复用共享文档客户端和原子变更路径。
- 操作目标已经过期时安全失败，不猜测新目标。
- 完成动作应幂等，或在重复执行时结果可预测。
- App、Widget 和其他 extension 读取同一本地真相。
- 写入后刷新相关快照，但不承诺系统无法保证的实时性。

## 5. P1：单一 `Now` Widget

### 5.1 进入条件

只有满足以下条件才开始 P1：

- 至少 4/5 测试用户独立完成首次激活。
- 至少 3/5 测试用户在 7 天中运行 4 天以上。
- `Now` 已被证明是最高频页面。
- 用户明确表达“希望不打开 App 也能看到当前状态”。

### 5.2 范围

先只验证：

- 一个 Widget kind。
- `systemSmall`。
- 当前 block、结束时间和最多 1–2 个 checklist。
- 整体点击打开 `Now`。
- 如果交互式完成稳定，可允许完成一条明确 checklist。

`systemMedium` 只有在 small 信息不足被真实观察到时才增加。

不同时开发：

- accessory 全家族。
- `systemLarge`。
- 多种可配置 Widget。
- Controls 或 Live Activity 配套。
- 模板切换和创建动作。

### 5.3 验收标准

- Widget 与 App 对当前 block 和 checklist 的理解一致。
- 跨 block 后在系统允许范围内更新，不长期显示已过期状态。
- 完成 checklist 后数据不丢失、不回滚。
- 无模板、未激活、空白时段和全部完成都有明确状态。
- Widget 的周使用率证明它减少了打开 App 查找当前状态的成本。

## 6. 冻结入口的恢复规则

任何冻结入口重新进入排期前，必须回答：

1. 它放大的是哪个已验证行为？
2. 当前行为的使用频率和用户证据是什么？
3. 为什么现有 App 或 Widget 入口不够？
4. 它会减少多少步骤或多少上下文恢复成本？
5. 最小可验证形态是什么？
6. 它是否增加维护、并发写入、权限或系统生命周期风险？

无法回答时继续冻结。

恢复顺序不预先承诺。默认按证据排序，而不是按以下技术清单排序：

- Lock Screen accessory widgets
- 本地通知
- Notification Actions
- Live Activities
- Controls
- App Shortcuts / Siri / Spotlight
- Quick Actions

## 7. 已有实现的维护边界

现有代码可以继续保留统一 route、action executor、App Intents、Live Activity controller、通知 coordinator 和 Widget 支持层。

维护时遵守：

- 不删除可靠能力只是为了让代码看起来与当前范围一样小。
- 不因已有抽象存在就继续添加入口。
- P0 修改不得被迫同时适配所有冻结入口；只有数据模型兼容和不崩溃是基本要求。
- 冻结入口如阻碍 P0 演进，可以隐藏、隔离或标记实验性，但删除需要单独决策。
- 系统入口测试保留为回归保护，不作为产品进度指标。

## 8. 回归检查

冻结期间至少保证：

- App 与 extension 能读取同一文档版本。
- 旧 deep link 解析不会导致崩溃或错误覆盖。
- 过期 task/block ID 的写操作安全失败。
- Widget、Live Activity 或通知在无数据时不崩溃。
- 可用性 gate 正确处理不同 iOS 版本。
- P0 的首次激活和默认运行不依赖任何系统入口。

## 9. 最终原则

系统入口的价值不在于“融入了多少 iOS 位置”，而在于是否让已验证的 `Now` 更容易被看见和执行。

> App 内核心闭环先成立；Widget 先证明放大价值；其余入口按证据逐项恢复。
